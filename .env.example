# Auth.js (Vercel) — generate: openssl rand -base64 32
AUTH_SECRET=
# Local dev only; Vercel sets this automatically in production
AUTH_URL=http://localhost:8080/api/auth

# Supabase JWT secret — Project Settings → API → JWT Secret (bridges admin RLS)
SUPABASE_JWT_SECRET=

# Supabase data + storage (not used for login anymore)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Optional bootstrap password for npm run bootstrap-admin
# ADMIN_BOOTSTRAP_PASSWORD=Hasal@2011
